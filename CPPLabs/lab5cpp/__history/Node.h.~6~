// ---------------------------------------------------------------------------

#ifndef NodeH
#define NodeH

class Node {
public:
	Node(T data = T()){this->data = data; this->prev = nullptr; this->next = nullptr;}
	~Node() {}
	Node *prev, *next;
	T data;
};
#endif
